---
import Card from "../ui/Card.astro";
---
<div class="relative flex flex-col-reverse items-center justify-center md:flex-row" id="container">
    <div class="w-full md:w-6/12 flex justify-center">
        <Card>
            <div class="flex flex-col h-[calc(100vh-12rem)] bg-white dark:bg-slate-800 rounded-lg shadow-sm border border-black">
                <div class="p-4 border-b border-black flex justify-between items-center">
                    <h2 class="text-xl font-bold text-black dark:text-white">Chat To the Deceased ...</h2>
                    <button
                        id="ClearTheConversation"
                        class="inline-flex items-center px-4 py-2 text-lg font-bold text-white bg-black rounded-md transition-colors duration-200 hover:bg-gray-800"
                        onclick="
                            const modelName = JSON.parse(localStorage.getItem('api-settings') || '{}')['model-name'];
                            if (modelName) {
                                localStorage.removeItem(`${modelName}-history`);
                            }
                            document.getElementById('chat-messages').innerHTML = '';
                            window.location.reload();
                        "
                    >
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
                        </svg>
                        Clear The Conversation
                    </button>
                </div>

                <div class="flex-1 overflow-y-auto p-6 space-y-4" id="chat-messages">
                    <div class="border border-black rounded-lg p-5 text-lg font-bold text-black dark:text-white">
                        Ten years of life and death are boundless, thoughtless, unforgettable.
                    </div>
                    <div class="border border-black rounded-lg p-5 text-lg font-bold text-black dark:text-white">
                        十年生死两茫茫，不思量，自难忘。
                    </div>
                </div>

                <div class="border-t border-black p-6 bg-slate-50 dark:bg-slate-800">
                    <form id="chat-form" class="flex gap-4">
                      <label for="message-input"></label><textarea
                            id="message-input"
                            class="flex-1 rounded-lg border-black bg-white dark:bg-slate-700 text-black dark:text-white shadow-sm focus:border-black focus:ring-black sm:text-lg px-4 py-2.5 resize-none font-bold"
                            placeholder="Type something..."
                            rows="1"
                        ></textarea>
                        <button
                            type="submit"
                            class="inline-flex items-center px-5 py-2.5 border border-transparent text-lg font-bold rounded-lg shadow-sm text-white bg-black hover:bg-gray-800 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-black transition-all duration-200 disabled:opacity-50 disabled:cursor-not-allowed"
                        >
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 5l7 7-7 7M5 5l7 7-7 7" />
                            </svg>
                            Send to..
                        </button>
                    </form>
                </div>
            </div>
        </Card>
    </div>
</div>
